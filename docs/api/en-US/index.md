# API Documentation (en-US)

This site is generated from C# XML comments and project metadata.

## Scope

- `Libro.LineMessageAPI`
- `Libro.LineMessageAPI.Extensions`

## Build

Use the repository script:

```powershell
pwsh scripts/build-api-docs.ps1 -Locale en-US
```
